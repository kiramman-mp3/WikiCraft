// MobsController.js (Controlador del lado cliente)
export function mobsPage() {
    // Lógica de mobs (obtenemos los datos y redirigimos al HTML correspondiente)
    window.location.href = '/mobs.html'; // Redirige a la página de mobs
}
